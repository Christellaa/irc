- check mode pour join un channel
- trunc nickname if > 9 in setNickname()






- return 0 or signal number when quitting with signal?

`
		const char* msg = "Server full. Try again later.\n";
		send(clientfd, msg, strlen(msg), 0);
		close(clientfd);
` => reused so need a util function
